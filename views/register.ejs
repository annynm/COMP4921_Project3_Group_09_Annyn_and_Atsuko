<!DOCTYPE html>
<html>

<head>
  <%- include('partials/header', { title: title }) %>
</head>

<body>
  <%- include('partials/nav') %>

    <div class="container">
      <div class="glass-card login-form">
        <h1 style="color: var(--greendale-blue-dark); text-align: center; margin-bottom: 2rem;">Create Your Account</h1>

        <% if (error) { %>
          <div class="glass-card"
            style="background: rgba(244, 67, 54, 0.1); border-color: var(--error); margin-bottom: 1rem;">
            <p style="color: var(--error); margin: 0;">
              <%= error %>
            </p>
          </div>
          <% } %>

            <div class="glass-card"
              style="background: rgba(0, 102, 204, 0.1); border-color: var(--greendale-blue); margin-bottom: 1.5rem; font-size: 0.85rem;">
              <p style="color: var(--greendale-blue-dark); margin-bottom: 0.5rem;"><strong>Password
                  Requirements:</strong></p>
              <ul style="margin: 0; padding-left: 1rem; color: var(--text-secondary);">
                <li>At least 10 characters long</li>
                <li>One uppercase letter (A-Z)</li>
                <li>One lowercase letter (a-z)</li>
                <li>One number (0-9)</li>
                <li>One special character (@ $ ! % * ? &)</li>
              </ul>
            </div>

            <form method="POST" action="/register" novalidate>
              <div class="form-group">
                <label for="username">Username</label>
                <input type="text" id="username" name="username" placeholder="Choose a username"
                  value="<%= locals.formData?.username || '' %>" required minlength="3" maxlength="30">
              </div>

              <div class="form-group">
                <label for="email">Email Address</label>
                <input type="email" id="email" name="email" placeholder="Enter your email"
                  value="<%= locals.formData?.email || '' %>" required>
              </div>

              <div class="form-group">
                <label for="fname">First Name</label>
                <input type="text" id="fname" name="fname" placeholder="Your first name"
                  value="<%= locals.formData?.fname || '' %>" required>
              </div>

              <div class="form-group">
                <label for="lname">Last Name</label>
                <input type="text" id="lname" name="lname" placeholder="Your last name"
                  value="<%= locals.formData?.lname || '' %>" required>
              </div>

              <div class="form-group">
                <label for="password">Password</label>
                <input type="password" id="password" name="password" placeholder="••••••••••••" required>
              </div>

              <div class="form-group">
                <label for="confirm_password">Confirm Password</label>
                <input type="password" id="confirm_password" name="confirm_password" placeholder="••••••••••••"
                  required>
              </div>

              <button class="btn btn-primary cta-button" type="submit" style="width: 100%; margin-top: 1.5rem;">
                Register Account
              </button>
            </form>

            <div style="text-align: center; margin-top: 1.5rem; font-size: 0.9rem;">
              <p style="color: var(--text-secondary);">
                Already have an account?
                <a href="/login" style="color: var(--greendale-blue); font-weight: 600;">Log in</a>
              </p>
            </div>
      </div>
    </div>

    <%- include('partials/footer') %>
</body>

</html>
